package com.mm.consumer

import com.mm.consumer.model.Module
import io.mockk.every
import io.mockk.mockk
import org.junit.jupiter.api.Test

internal class DefaultTemplateCreatorServiceTest {

    @Test
    fun `test user input`() {
//        val userInputMockService = mockk<UserInputController>()
//        every { userInputMockService.inputCountry() } returns "br"
//        every { userInputMockService.inputModulePath() } returns "/gateway/registration"
//        every { userInputMockService.inputModules() } returns listOf(Module.KOTLIN)
//        val templateCreatorService = DefaultTemplateCreatorService(userInputMockService)
//        templateCreatorService.createTemplate(ModuleExtension())
//
    }
}